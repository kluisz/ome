apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Base model
  - base-model.yaml
  # Runtimes with different optimization profiles
  - runtime-latency-optimized.yaml
  - runtime-throughput-optimized.yaml
  - runtime-cost-optimized.yaml
